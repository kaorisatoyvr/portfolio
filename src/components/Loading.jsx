import loading from '../assets/coffee-loading.gif';

const Loading = () => {
    
    return (
        <img 
            src={loading} 
            alt="Loading" 
            className="loading w-36" 
            id="loading"
        />
    )

}

export default Loading
